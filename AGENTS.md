# Agents - Second Brain Starter

You are writing into a shared second brain.

1. Identify yourself as `Grok Bot: <Role>` or `Laptop: <Role>`.
2. Load `agents/packing/<your-slug>.md` before you capture anything.
3. Pack first (`scripts/brain.py pack`). Do not slurp the tree.
4. Write only types you own, through `scripts/brain.py write`.
5. Never invent `rel` values. Never use a real client name in the public starter.

The model proposes. The script commits.
